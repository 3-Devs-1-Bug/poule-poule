# see https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  base = "client/"
  publish = "build/"
  command = "yarn build"

# Redirect API calls
[[redirects]]
  from = "/api/*"
  to = "https://poule-poule-api.azurewebsites.net/:splat"
  status = 200

